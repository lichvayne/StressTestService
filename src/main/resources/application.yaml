spring:
  application:
    name: Stress-test
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/users
server:
  port: 8081

